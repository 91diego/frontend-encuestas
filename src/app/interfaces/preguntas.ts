export interface Preguntas {

    // ATRIBUTOS DE LA INTERFAZ
    id?: number;
    numero: number;
    descripcion: string;
    multiple: boolean;
    encuesta_id: number;
    created_at?: string;
    updated_at?: string;
}
